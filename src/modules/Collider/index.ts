// function distanceTo (object, target) {
//     const dx = (object.pos.x + object.mesh.radius) - (target.pos.x + target.mesh.radius);
//     const dy = (object.pos.y + object.mesh.radius) - (target.pos.y + target.mesh.radius);

import GameObject from "../../gameObjects/GameObject";

//     const distance = Math.sqrt(dx * dx + dy * dy);

//     return distance;
// }


function distanceTo (object: GameObject) {
    
}